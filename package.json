{
  "name": "@sym-bot/mmp-spec",
  "version": "2.0.0",
  "private": true,
  "description": "Public mirror of the machine-readable contract and conformance vectors for MMP v2.0",
  "type": "module",
  "scripts": {
    "generate:vectors": "node scripts/generate-vectors.mjs",
    "verify:mirror": "node scripts/verify-mirror.mjs",
    "verify": "npm run verify:mirror && node scripts/verify-vectors.mjs",
    "test": "npm run verify"
  },
  "license": "CC-BY-4.0",
  "engines": {
    "node": ">=20"
  },
  "devDependencies": {
    "ajv": "^8.17.1",
    "ajv-formats": "^3.0.1"
  }
}
